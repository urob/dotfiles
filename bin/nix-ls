#!/usr/bin/env bash

readlink -f /nix/store/* |
    grep '/nix/store/' |    # filter out non-store paths
    grep -v '.drv$' |       # filter out derivation sources
    grep -v -e '-man$' |    # filter out man pages
    grep -P "^.*?-$1" |     # extract requested, if any
    sort -u
